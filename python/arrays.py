# continuouse space of bytes
# memory needs instructions to be interpreted
# array is a chunk of memeory understood as an array

# array buffer as list in python has kind of weird array operations
a: [int] = [0] * 6
a[0] = 45
a[2] = 3
print(a)

